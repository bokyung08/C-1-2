/*#include <stdio.h>
#include <stdlib.h>


int main() {
    int numbers[100];
    int count = 0;
    int num;

    printf("input:\n");

    while (1) {
        scanf("%d", &num);
        if (num == -1) {
            break;
        }
        numbers[count++] = num;
    }

    for (int i = count - 1; i >= 0; i--) {
        printf("output: %d ", numbers[i]);
    }

    return 0;
}
*/