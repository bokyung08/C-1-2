/*#include <stdio.h>
struct position {
	int x;
	int y;
};
int main() {
	struct position pos = { 10,20 };
	struct position* ptr;
	ptr = &pos;
	printf("%d\t %d\n",(*ptr).x, (*ptr).y);
	printf("%d \t %d\n", ptr->x, ptr->y);

	return 0;

}
*/