/*#include <stdio.h>
int main() {
	FILE* fw = fopen("output.txt", "w");
	if (fw == NULL) {
		printf("file open failed");
		return 1;
	}
	fprintf(fw, "you can't see this text");
	fflush(fw);// 파일에 쓴 내용이 저장됨
	int temp;
}*/